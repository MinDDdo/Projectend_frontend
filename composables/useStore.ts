import { authStore } from '~/store/auth.store';
import { teacherStore } from '~/store/teacher.store'; 
import { authStudentStore } from '~/store/auth-student.store';
import { studentStore } from '~/store/student.store';

export default {
    authStore,
    teacherStore,
    authStudentStore,
    studentStore
}